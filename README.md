# VK Bot для типографии "Технология"

Бот для приема заказов, расчета стоимости и предоставления информации.

## 🚀 Быстрый запуск (Windows)

1. Убедитесь, что у вас установлен **Python** (версии 3.8 или новее).
2. Запустите файл `run_bot.ps1` (правой кнопкой мыши -> "Выполнить с помощью PowerShell").
3. Бот начнет работу.

## ⚙️ Настройка

Основные настройки находятся в файле `.env`:

- `VK_TOKEN`: Токен вашей группы ВКонтакте.
- `ADMIN_ID`: ID пользователя ВК (цифры), которому будут приходить уведомления о заказах.

Настройки текстов и контактов можно изменить в файле `config.py`.

## 📂 Структура

- `main.py` - Основной файл запуска.
- `data/prices.py` - Цены на услуги (редактировать тут).
- `services/calculator.py` - Логика расчета.
- `handlers.py` - Обработка ответов бота.
